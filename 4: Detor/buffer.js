/*
   ? What is Buffer?
    * Buffer is a temporary storage spot for a chunk of data that is being transferred from one place to another.
    * Buffer is used for storing binary data.
    * Buffer is used for converting data from one format to another.
    * Buffer is used for streaming data.
    * Buffer is used for implementing file uploads and downloads.
    * Buffer is used for handling TCP streams.
    * Buffer is used for handling UDP streams.
    * Buffer is used for zlib streams.
    * Buffer is used for crypto streams.
    * Buffer is used for handling child process streams.    
*/
